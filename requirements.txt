# Core HTTP / env
requests>=2.31.0
python-dotenv>=1.0.0

# Google Drive upload
google-api-python-client>=2.100.0
google-auth-httplib2>=0.1.1
google-auth-oauthlib>=1.1.0

# AI — GPT-4o via GitHub Models
openai>=1.30.0

# AI — Groq (Llama 3 captions)
groq>=0.5.0

# Image processing (White Meme Card)
Pillow<10.0.0

# Video editing (MoviePy assembly)
moviepy<2.0
imageio>=2.34.0
imageio-ffmpeg>=0.4.9
numpy>=1.26.0

# Video sourcing (Reddit .json + YouTube Shorts)
yt-dlp>=2024.3.10

# Speech-to-text (word-level timestamps)
faster-whisper>=1.0.0
